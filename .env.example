# Base path for deployment
# Root domain (Vercel, Netlify, custom domain): leave as / or unset
# GitHub Pages project site (username.github.io/repo-name/): /lingualearn/
# VITE_BASE_PATH=/

# Backend API URL (production / split deploy)
# Local dev: leave unset — Vite proxies /api to http://localhost:3001
# Vercel/Netlify: set to Railway BACKEND service URL (HTTPS), e.g.:
# VITE_API_URL=https://lingualearn-api-production.up.railway.app
#
# WRONG — do NOT use Postgres connection string here:
# VITE_API_URL=postgresql://postgres:...@....proxy.rlwy.net:54577/railway
