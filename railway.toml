[build]
builder = "nixpacks"
buildCommand = "cd backend && npm ci && npm run railway:build"

[deploy]
startCommand = "cd backend && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
